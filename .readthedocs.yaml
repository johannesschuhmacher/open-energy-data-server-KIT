# .readthedocs.yml

# Required fields
version: 2

build:
  os: "ubuntu-24.04"
  tools:
    python: "3.14"

sphinx:
  configuration: docs/source/conf.py

python:
  install:
    - requirements: docs/requirements.txt
