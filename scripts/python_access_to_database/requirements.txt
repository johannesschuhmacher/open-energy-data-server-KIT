#<!--SPDX-FileCopyrightText: Johannes Schuhmacher-->

#<!--SPDX-License-Identifier: AGPL-3.0-or-later-->

# ───────── Kern-Abhängigkeiten ─────────
pandas>=2.2,<3.0            # DataFrame-Handling
SQLAlchemy>=2.0,<3.0        # ORM / DB-Abstraktion
python-dotenv>=1.0,<2.0     # .env laden

# ───────── PostgreSQL-Treiber (nur **einen** auswählen) ─────────
psycopg[binary]>=3.2,<4.0    # moderner Psycopg 3, C-Binary -↴
# psycopg2-binary>=2.9,<3.0  # …oder klassischer Psycopg 2

# ───────── SSH-Tunnel ─────────
sshtunnel>=0.4,<0.5          # Port-Forwarding
paramiko>=2.12,<3.0          # SSH-Backend  (v3 entfernt DSSKey)

# ───────── Optionale Tools (falls interaktiv) ─────────
# ipython>=8.0               # nette REPL-Erweiterung
# uv pip install -r requirements.txt
